NEXT_PUBLIC_APP_NAME="Solar Tank Monitoring System"
NEXT_PUBLIC_APP_ENV="development"

# Local-only placeholder values.
# Never commit real database URLs, device API keys, webhook secrets, SMTP secrets,
# WiFi credentials, deployment tokens, or production domains.

# DATABASE_URL="file:./local-dev.db"
# DEVICE_INGEST_API_KEY="replace-with-local-development-key"
# SIMULATOR_DEFAULT_DEVICE_ID="demo-tank-001"
# SIMULATOR_DEFAULT_SITE_ID="demo-site-001"
